server:
  port: ${SERVER_PORT}

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
          - id: user-service
            uri: ${USER_SERVICE_URL}
            predicates:
            - Path=/api/users/**

          - id: board-service-jwt
            uri: ${BOARD_SERVICE_URL}
            predicates:
            - Path=/api/boards
            - Method=POST
            filters:
            - JwtAuthenticationFilter

          - id: board-service
            uri: ${BOARD_SERVICE_URL}
            predicates:
            - Path=/api/boards/**
